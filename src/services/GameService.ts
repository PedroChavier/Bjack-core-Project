// TODO: implementar GameService
// Responsável pelas regras de negócio da partida (valida apostas, aplica efeitos de joker, etc.)
// e acesso ao banco para persistir estado da sessão.

export class GameService {
    // static async startGame(userId: number) { ... }
    // static async hit(sessionId: string) { ... }
    // static async stand(sessionId: string) { ... }
}
