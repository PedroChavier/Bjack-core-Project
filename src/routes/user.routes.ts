import { Router } from 'express';

const router = Router();

// TODO: registrar rotas de usuário
// router.post('/', UserController.create);
// router.get('/:id', UserController.getById);

export default router;
