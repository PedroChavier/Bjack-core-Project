import { Router } from 'express';

const router = Router();

// TODO: registrar rotas do jogo
// router.post('/start', GameController.startGame);
// router.post('/hit',   GameController.hit);
// router.post('/stand', GameController.stand);

export default router;
